services:
  - type: web
    name: verus-server
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    envVars:
      - key: PORT
        value: 10000
      - key: MODEL_GDRIVE_URL
        sync: false   # set this in Render dashboard → Environment
