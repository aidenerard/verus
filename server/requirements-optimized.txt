# Optimized requirements for Render free tier (512MB RAM limit)
# Uses CPU-only PyTorch which is ~500MB smaller than full version

# Core dependencies
fastapi==0.115.12
uvicorn[standard]==0.34.5
python-multipart==0.0.20

# CPU-only PyTorch (much smaller than CUDA version)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.0+cpu

# Scientific computing
numpy==1.26.4
pandas==2.2.3
scipy==1.14.1

# Image processing (base version, no GUI)
matplotlib==3.9.3
pillow==11.1.0

# Additional
gdown==5.2.0
