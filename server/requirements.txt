# Minimal dependencies for 512MB RAM + limited disk space

# Core dependencies
fastapi==0.109.0
uvicorn==0.27.0
python-multipart==0.0.7

# Scientific computing (use older, smaller versions)
numpy==1.24.3
pandas==2.0.3
scipy==1.10.1

# Image processing
matplotlib==3.7.2
pillow==10.0.0

# Utilities
gdown==4.7.1
psutil

# Lightweight PyTorch CPU-only
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.0.1+cpu
