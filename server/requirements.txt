# Pinned for Python 3.11 on Render free tier (512 MB RAM)
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.7
gunicorn==21.2.0

# Scientific computing — all have Python 3.11 wheels
numpy==1.26.4
pandas==2.1.4
scipy==1.12.0

# Plotting
matplotlib==3.8.2
pillow==10.2.0

# Utilities
psutil==5.9.8
gdown==4.7.1

# GPR format parsers (auto-installed on first use if absent; pinned here for reproducibility)
readgssi>=0.0.18
segyio>=1.9.12

# PyTorch CPU-only — lightweight for 512 MB RAM
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.2+cpu
